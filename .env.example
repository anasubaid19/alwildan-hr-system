# Server
PORT=3010
NODE_ENV=development

# Database path (relatif dari backend/)
DB_PATH=./data/alwildan.db

# JWT — ganti dengan string acak yang panjang di production
JWT_SECRET=

# AI Provider (diisi via halaman Settings, bisa juga di-override lewat .env)
AI_BASE_URL=
AI_API_KEY=
AI_MODEL=
